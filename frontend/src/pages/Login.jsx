import LoginForm from "../components/LoginForm";

const Login = () => {
  return (
    <div
      className="d-flex align-items-center justify-content-center bg-primary p-3 p-lg-0"
      style={{ height: "100dvh" }}
    >
      <LoginForm />
    </div>
  );
};

export default Login;
